Kubernetes Pod scaler 
- Horizodntal pod scaler: Horizontal scaling means that the response to increased load is to deploy more Pods.
- Vertical pod scaler : Kubernetes would mean assigning more resources (for example: memory or CPU) to the Pods that are already running for the workload.

cpu memory or custom matrics 

traffic load increase you need scale services on basis of matrics 