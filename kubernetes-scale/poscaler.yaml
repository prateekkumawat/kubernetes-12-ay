apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: apploader-scaler
  namespace: scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: apploader
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
